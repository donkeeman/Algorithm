1, 3, 5

// i = n이면 최악의 경우 O(n)
// 어차피 Big O는 상수를 무시하므로 O(n)
// arr[i]

// 무조건 마지막 인덱스에 원소를 삽입하므로 O(1)
// arr.push(5);

// slices()->매개변수가 없는 경우 원본 배열을 복사함
// 복사하려면 원소 전체를 검사해야 하므로 O(n)
// arr.slice();

// 무조건 마지막 인덱스의 원소를 제거하므로 O(1)
// arr.pop();

// includes(n)->array 안에 n이라는 원소가 있는지 검사
// 최악의 경우 원소 전체를 검사하므로 O(n)
// arr.includes(5);
