var nums = [100, 200, 300, 400, 500];

// 둘 다 같은 동작을 한다

// pop(): 가장 마지막 인덱스의 원소를 제거. 스택이랑 같은 함수
// nums.pop();
// nums.pop();

// splice(i, j): 인덱스 i로부터 j개의 숫자를 제거(j = 1이면 array[i]의 원소 1개 제거)
// nums.splice(4, 1);
// nums.splice(3, 1);

console.log(nums);